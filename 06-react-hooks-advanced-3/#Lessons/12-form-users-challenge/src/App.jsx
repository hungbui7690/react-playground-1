/*
  User Challenge
  1. setup controlled input (name input)
  2. setup onSubmit (for now just placeholder)
  3. import data array (first array) from data
    - create another state value (data as default)
  4. iterate over and display right after form (h4)
  5. when user submits the form add new person to the list
  6. add button and setup functionality to remove user


*/

import Starter from './02-user-challenge'

const App = () => {
  return (
    <main>
      <Starter />
    </main>
  )
}
export default App
