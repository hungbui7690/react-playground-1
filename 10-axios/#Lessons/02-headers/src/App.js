/*
  Setup Headers
  - last argument is option
    > axios.get(url,{})
    > axios.post(url,{data},{})


*/

import Title from './components/Title'
import Setup from './2-headers'

function App() {
  return (
    <main>
      <Title />
      <Setup />
    </main>
  )
}

export default App
