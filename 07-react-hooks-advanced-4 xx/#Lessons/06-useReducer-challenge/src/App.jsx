/*
  useReducer
  > Challenge:
    - add reset button
      > reset will add all data back to UI
    - if everything is clear:
      + hide "clear" button
      + show "reset" button

*/

import Starter from './01-useReducer'

const App = () => {
  return (
    <main>
      <Starter />
    </main>
  )
}
export default App
