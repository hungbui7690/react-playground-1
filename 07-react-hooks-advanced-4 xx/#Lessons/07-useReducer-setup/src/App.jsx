/*
  useReducer - Setup
  - action.type

    dispatch({type:'SOME_ACTION'})
    > handle it in reducer
    > return new state

*/

import Starter from './01-useReducer'

const App = () => {
  return (
    <main>
      <Starter />
    </main>
  )
}
export default App
