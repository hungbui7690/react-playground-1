/*
  Slider - Current Person
  - Create a currentPerson state value in App.jsx and set it to 0 initially. 
    > This will allow you to keep track of the current slide being displayed.

  - Prev and Next
    > Implement the prev and next functionality using the setCurrentPerson function to update the currentPerson state value.

    (1) Carousel.jsx

*/

import Carousel from './Carousel.jsx'

const App = () => {
  return (
    <main>
      <Carousel />
    </main>
  )
}
export default App
