/*
  Tours - Setup
  - pic >> requirement
  
  - First create - three components (Tours, Tour, and Loading), you can create three separate files in your project directory: Tours.jsx, Tour.jsx, and Loading.jsx. 
    > In each of these files, you will define a React functional component that returns JSX code for rendering the respective component.

*/

import './index.css'

const url = 'https://course-api.com/react-tours-project'

const App = () => {
  return <h2>Tours Starter</h2>
}
export default App
